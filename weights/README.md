Place the MedSAM ViT-B checkpoint here:

```
weights/medsam_vit_b.pth
```

Download from the [MedSAM repository](https://github.com/bowang-lab/MedSAM) and rename the file to `medsam_vit_b.pth`, or pass a custom path via `--medsam_weights` when training SAM-based models.
